apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app-deployment

spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-app
  template:
    metadata:
      labels:
        app: demo-app
    spec:
      containers:
        - name: demo-app
          image: ghcr.io/nest-analytics/demo-app:3d34c5ca0fjfffnf
          ports:
            - containerPort: 3000
  
